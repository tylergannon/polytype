# Issue #127 final independent validation

**Target:** `19b56ce` on `codex/issue-127-release`, checked against [issue #127](https://github.com/tylergannon/polytype/issues/127), `ephemeral/issue-127-plan.md`, and the authorized deferral of recursive JSON Schema and validation.

**Outcome: no findings. Readiness: YES** for the scoped finite recursive Go type release. No material defect remains in the reviewed behavior. The original issue's recursive JSON Schema request remains explicitly deferred.

The final change in `internal/builder/gen_schema.go` follows the underlying expression of named non-struct types, while the visited set prevents recursive traversal. The focused `internal/builder/named_container_test.go` exercises both schema-backed and schema-free discovery. I independently copied the round-3 failing programmatic fixture, pointed its module at this delivery worktree, generated once, and ran it again in a separate Go invocation so the compiler included the newly written codec. The second `go run ./cmd/probe` exited 0 and printed `{"items":[{"children":[{"children":[],"blocks":[{"text":"inner","type":"Leaf"}]}],"blocks":[{"text":"outer","type":"Leaf"}]}]}`. Both recursively nested union values carry their required discriminator. This directly resolves round 3's silent missing-discriminator failure.

I inspected the relevant implementation diff, issue, plan, prior three validation rounds, recursive integration tests, and generated codec paths. The required pre-review `go test ./...` passed. Final uncached `go test -count=1 ./...`, `just build-tagged`, and `git diff --check 19b56ce^ 19b56ce` passed. The uncached suite exercised recursive Go JSON/devalue round trips, pinned JavaScript devalue exchange, strict TypeScript compilation, deterministic regeneration, schema refusal before writes, package-qualified discovery, nested error propagation, and recursive embedding termination. The first independent fixture invocation compiled before its generated codec existed; its stale wire is a known two-invocation probe artifact, and the second invocation passed.

No source or durable tests were changed during this review. The temporary independent fixture was archived on the original branch under `ephemeral/issue-127/final-validation-scratch/`; its inherited round-3 capture records the old failing state, while the successful second invocation is recorded above.
