# Material findings

1. **High — the backend rejects an admitted `Object` shape, and its coverage guard masks the missing case.** The exported grammar lowers an inline struct field to `*typegrammar.Object` (`internal/builder/typegrammar.go`), but `codegen.Generate` fails as soon as that object is used below a definition because `goType` rejects every anonymous object (`devalue/codegen/generate.go:215-237`). An overlay bite test built a valid `Envelope` definition with a required inline `{Text string}` field; `Definitions.ValidateWithRoots` accepted it, then `Generate` returned `an anonymous struct type is supported only as a definition's own type`. This is a user-visible failure for an existing admitted Go shape (the repository already has nested inline structs in `internal/builder/test_run/test4-structs/struct_types.go`) and contradicts #105/the brief's requirement to generate codecs for every grammar node kind. `assertCoversEveryNodeKind` does not demonstrate that requirement: it marks `Object` covered by walking the top-level named definition objects (`devalue/codegen/generate_test.go:94-164`), while the codec fixture contains no inline object field. Add an inline-object consumer case and generate its encode/decode logic without requiring the anonymous type to be named in the sibling package.
