# Material findings

- **The branch commits a forbidden proof directory.** The brief's ground rules
  explicitly prohibit proof directories, but
  `ephemeral/devalue-projection/proof/106-examples.md` is committed on this
  branch as a hand-recorded proof report. Remove that directory/file; the
  permitted Go tests and required gates already demonstrate #106.
