# Issue 102 worklog

decision: Baseline at `38f05b1` is clean under `go test ./...`; issue #102 requires generation to stop silently removing previously requested validation output when `--validate` is omitted.

friction: A copied nested-module fixture retained a depth-relative `replace` directive and could generate from cached package data but could not run `go test` at the new depth -> runtime proof fixtures must rewrite the replacement to the task worktree's absolute path.

decision: Use issue #102's explicit-force safety option rather than persisted sticky state: a generated `ValidateJSON` or `ValidateYAML` method may only be removed when `Force` is true, and the guard runs before schema writes.

proof: Compiled CLI generated a consumer with `--validate`; rerunning without it exited 1, preserved `jsonschema_gen.go` at SHA-256 `64f9e8c6f7963c83a94a880d7c56823276af8772e39d230c5034fd173f3a51f5`, and the consumer still passed `go test ./...`; `--force` then exited 0 and removed `ValidateJSON`.
