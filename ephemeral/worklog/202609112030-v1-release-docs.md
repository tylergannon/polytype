# v1 stable release documentation

decision: Prepare the durable v1 migration guide and reviewable stable release notes after the published rc.13 consumer proof. Do not tag stable until the maintainer chooses a license and the license file is merged.

change: Added one migration guide covering module/tool installation, fluent declarations, explicit presence, inferred enums and unions, YAML removal, generated-output ownership, validation-removal safety, TypeScript directory ownership, and verification. Linked it from the README, docs index, and website navigation.

artifact: Draft stable GitHub release notes live under `ephemeral/release/v1.0.0-notes.md` until the exact stable commit and license are known.

github: A live scan found one issue on the 1.0 milestone: #66. The only other open pull request is draft #75, which is not on the milestone and covers post-1.0 capability work.

proof:

- `go generate ./...` passed.
- `git diff --check` passed.
- `npm run check` in `website/` built the migration page and resolved every internal link.
- `check-doc-indexes.py` passed.
- `go vet ./...` passed.
- `just build-tagged` passed.
- `go test ./...` passed after the documentation changes.
