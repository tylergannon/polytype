# Usability deep dive

decision: Review current construction, presentation, and functionality; rank three material usability findings. User specifically proposes programmatic generation without marker registration files. No product implementation is requested.
decision: Continue the supplied clean task worktree at 964f778 on codex/usability-deep-dive. Keep review probes and evidence under ephemeral/.
proof: Required baseline `go test ./...` passed before exploration. Repeat after review and use focused executable probes for findings; do not change production tests solely for a read-only review.
finding: Public grammar.Load/Lower, typescript.Generate, and devalue/codegen.Generate exist. Declaration[T] is empty and fluent marker calls discard arguments. Scope the missing library API to JSON Schema, generated Go methods, and unified generation/configuration rather than claiming all programmatic generation is absent.
finding: README:661 and custom-backend guide claim every backend consumes typegrammar, but default schema rendering uses a separate model. The CLI calls TypeDefinitions only for requested TypeScript output. Portable admission and schema rendering differ.
proof: Five characterization tests under ephemeral/usability-deep-dive passed. Consumer execution proves uint8 slice schema rejects Go's base64 JSON; fixed-array schema admits extra elements that json.Unmarshal silently drops; --no-changes rewrites altered/missing schema while returning success; a Go output-path collision fails after JSON and sum writes. Marker-free TypeScript and devalue generation works.
correction: Initial byte-slice probe used []byte, which fails schema generation earlier with "mapNamedType: type byte not found". Changed probe to []uint8, an equivalent Go byte-slice spelling that reaches the accepted-but-wrong schema path. Do not report []byte itself as successfully generated.
decision: Favor a shared configuration and render-result API with marker scanning as an input adapter. Merely retaining current marker arguments cannot recover field identity from evaluated zero values; explicit source symbols or field names are necessary.
proof: `just build-tagged` passed. Final findings and replayable probes are under ephemeral/usability-deep-dive/. No production changes or issue/PR publication.
proof: Final repository `go test ./...` passed. All five characterization probes passed separately; they deliberately assert observed defects and are not production regression tests.
decision: Closeout on codex/usability-deep-dive with a local checkpoint containing only review findings, evidence, the standalone probe module, and this worklog. No push, PR, or product implementation.
proof: Local checkpoint pre-commit golangci-lint passed with 0 issues. Evidence log trailing whitespace normalized without changing diagnostic text.
decision: User requested the overall solution, proposed grammar/configuration language, examples, and 7-10 implementation steps. Proposal is at ephemeral/usability-deep-dive/programmatic-generation-proposal.md; it is not implementation authorization or an accepted nlspec.
decision: Recommend an executable Go DSL producing ordinary Config values, explicit qualified symbols/field names, a shared admitted static grammar, and Compile/Render/Check/Apply separation. Legacy AST scanning is an explicit adapter with its current CLI defaults.
decision: Include loader bootstrap overlays and already-loaded-package inputs in the design. Merely exporting builder.Run leaves the source-registration and missing-generated-method dependency unsolved. Keep provider plans explicit and reject requests needing unknown static provider output.
proof: Required baseline `go test ./...` passed before proposal work. Only proposal/worklog text is added for this follow-up; no production code or existing tests changed.
proof: Final `go test ./...` and `git diff --check` passed after proposal work. Local proposal checkpoint remains on codex/usability-deep-dive; no push or PR.
correction: User rejects two declaration APIs converging internally. EXACTLY the same polytype functions must return real configuration values in schema.go bindings and external generators. Breaking or completely reinventing the old marker syntax is authorized; prior legacy-AST-adapter recommendations are superseded.
decision: Revised proposal uses shared lightweight polytype.Config constructors, codegen.Gen as the consumer, named binding collection through ordinary Go evaluation, explicit field identities, and one declaration reference. No permanent no-op marker or AST-only option language.
decision: Initial acceptance gate now compares an identical expression executed in a tagged binding and an external generator. Collection of an unbuildable model requires an explicit bootstrap strategy; direct symbol-based configuration does not import that model. Proposal distinguishes these cases.
proof: Required baseline go test ./... passed before this proposal correction. Only proposal and worklog text changed.
correction: User clarified that this task is only high-level instructions, requirements, and a definition of done, not implementation or detailed API design. Replaced the detailed proposal with outcome, non-negotiable requirements, grammar boundaries, nine steps, and behavioral acceptance criteria. No production code changed.
correction: User requires a SHORT proposal with suggested formalities, not detailed specs. Reduced the proposal to a compact brief: one shared executable API, four suggested boundaries, nine one-line steps, and five done criteria. Detailed API and collection design removed.
proof: Final go test ./... and diff whitespace check passed. Proposal is 363 words; only proposal/worklog Markdown changed. Local correction checkpoint on codex/usability-deep-dive, no implementation, push, or PR.
