# Issue 100 wire contract

decision: User accepted making encoding/json/v2 the canonical Polytype wire encoder if a runtime spike confirms nil slices normalize to arrays through both direct v2 calls and generated MarshalJSON methods invoked by v1 callers.
correction: Keep issue 100 focused on direct bare-pointer fields and omission-tag diagnostics; the issue body's slice observation prompted a separate encoding-contract decision rather than an assumed schema widening.
correction: Once the user accepted the restrictive direct-field contract, do not re-open that decision as a question; ask only when the remaining public contract is genuinely underivable.
decision: The spike confirmed v2 defaults recursively encode nil slices as arrays. Generated owner codecs use DefaultOptionsV1 plus FormatNilSliceAsNull(false), preserving v1 behavior except for slice normalization. Handwritten nested MarshalJSON methods remain authoritative.
discovery: Embedded fields need the same direct-pointer and ordinary omission-tag checks as named fields because an embedded pointer can omit promoted required properties when nil.
proof: Focused builder tests pass for direct and embedded diagnostics, recursive nil-slice normalization, v1 callers of generated codecs, schema validation of generated bytes, and nested custom JSON hooks. Two consecutive go generate runs produced the same diff hash, 994c0aa276c7efed8c16ba37f6ba203a9af7394542fd7431da6ac54bb3f06998.
friction: Two idempotency assertion wrappers mistyped the shell variable in the final comparison. Both printed identical hashes but exited 1; a corrected rerun exited 0 with matching b2ae71b1b73d1a11fbfad8ac7948c74d7823e416666e68d71c5d7c8c3c2f0fae hashes. The first worklog patch also targeted a mistyped path and made no change before this successful patch.
review: PR #118 is mergeable. A local review found no repair-required defect, the branch diff passes git diff --check, and the initial GitHub build-and-deploy and test-and-generate checks both passed.
