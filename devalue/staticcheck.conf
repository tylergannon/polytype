# The devalue port reproduces the upstream JavaScript library's error messages
# verbatim ("Invalid input", "Cannot stringify ..."), and its tests assert
# them, so ST1005's lower-case rule does not apply in this tree. Everything
# else inherits the repository defaults.
checks = ["inherit", "-ST1005"]
